[project]
name = "fo-annotate-lerobot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fiftyone>=1.17.0",
    "pyarrow>=24.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
